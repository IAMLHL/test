@{
    ViewBag.Title = "About";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<p>Use this area to provide additional information.</p>
<img src="/Home/Image" />
@ViewBag.abc
--------------------------------------
@ViewBag.def
@Html.ActionLink("Image", "Image")
@{
    int[] num = new int[10] { 2, 3, 1, 9, 7, 4, 5, 6, 0, 8 };

    //for (int i = 0; i < num.Length-1; i++)
    //{
    //    for (int j = i+1; j < num.Length; j++)
    //    {
    //        if (num[j]<num[i])
    //        {
    //            int temp = num[i];
    //            num[i] = num[j];
    //            num[j] = temp;
    //        }
    //    }
    //}

    for (int i = 0; i < num.Length - 1; i++)
    {
        for (int j = num.Length - 1; j > i; j--)
        {
            if (num[j] < num[j - 1])
            {
                num[j] = num[j] + num[j - 1];
                num[j - 1] = num[j] - num[j - 1];
                num[j] = num[j] - num[j - 1];
            }
        }
    }

    string sw = "";
    foreach (var item in num)
    {
        sw += item + ",";
    }
    <p>@sw</p>
}